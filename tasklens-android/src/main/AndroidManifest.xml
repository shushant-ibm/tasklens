<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for NetworkMonitor to observe connectivity changes -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Required for ForegroundServiceAdapter (declared in tasklens-foreground, but noted here for clarity) -->
    <!-- Required for AlarmManagerAdapter and JobSchedulerAdapter receivers -->

</manifest>
